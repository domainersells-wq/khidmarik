// src/app/reservations/layout.tsx

export default function ReservationsLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* Reservations Layout (e.g., Sidebar) */}
      {children}
    </div>
  );
}
